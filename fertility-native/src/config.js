// API Keys — fill in before the demo
// For production: use environment variables via EAS / expo-constants

export const CONFIG = {
  // Mistral API key — get yours at https://console.mistral.ai
  MISTRAL_API_KEY: '',

  // Thryve API key (Phase 2) — get yours at https://docs.thryve.health
  THRYVE_API_KEY: '',

  // Set to true to always use mock data (useful for offline demo)
  FORCE_MOCK: false,
}
